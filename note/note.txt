コンビニの店舗を調査して、調査結果を登録するWebアプリがある。
調査対象の店舗を選ぶポップアップ画面を新しく作りたい。

投稿者が、調査対象の店舗を選ぶ際に、すでに店舗マスタにある店舗を選ぶこともできるが、店舗マスタに登録していない任意の店舗を登録できるようにしたい。

画面項目について
・店舗名の入力欄
　店舗名をいれると、すでにDBに登録してある店舗名を nm_cvs_store like '%入力値%' でAjax検索し、類似の店舗名を候補としてサジェストする。
　　※サーバ側の実装は別途行う。expressでのモックサーバを用意してほしい。
　店舗名は入力ブレが想定されるが、あくまで店舗の正式名を入れる前提であると考え、複雑なサジェスト向け検索などはしなくてよい。

・チェーン名、立地条件（駅前、繁華街、郊外etc）、都道府県　をリストから選択
　店舗名入力欄で、サジェストから既存の店舗を選んだ場合、チェーン名・立地条件・都道府県　はその店舗の内容を表示する。
　お気に入り店舗から店舗を選んだ場合も同様。
・お気に入り店舗
　あらかじめ、他画面でCookieにお気に入り店舗名のリストをもたせる。
　お気に入り店舗名のリストを表示しておき、そこから店舗を選ぶこともできる。
　最大10件程度を想定。




まとめ
c:/my/dev/20260817_simple に、調査対象店舗選択ポップアップ一式をゼロから構築しました。

server.js — Expressモックサーバ。GET /api/stores/suggest?q=（店舗名部分一致サジェスト）、GET /api/options（チェーン名/立地条件/都道府県の選択肢）、POST /api/target-store/register（登録）
data/stores.js — 店舗マスタのダミーデータ28件＋選択肢マスタ
public/index.html — ホスト画面（デモ）＋ポップアップ（モーダル）のマークアップ
public/js/store-select.js — ポップアップ本体のロジック（サジェスト・お気に入り・登録）
public/js/main.js — ホスト画面側ロジック（お気に入りCookieの検証用シード、選択結果の受け取り表示）
実機でPlaywrightを使いブラウザ操作を通してお気に入り選択→サジェスト検索→未登録店舗の手動登録まで一通り検証し、その過程で jQuery 1.7.2では$.ajaxのmethodオプションが効かず(typeが必要)、登録APIがGETになり404していたバグを発見・修正済みです。検証用に一時導入したPlaywrightとスクリプト・スクリーンショットは削除済みで、package.jsonの依存関係もExpressのみのクリーンな状態です。

npm start（またはnode server.js）で起動し、http://localhost:3000で動作確認できます。




＝＝＝＝＝＝＝

お気に入りを追加するポップアップ画面も用意したい。
・テキスト入力欄を10個並べる
・テキスト入力欄は、店舗登録時の店舗名欄と同等のサジェストができる
・選択したら、ブラウザのcookieに保持する


実装内容
「お気に入り店舗を編集」ポップアップを新規追加しました。

public/js/favorite-edit.js — テキスト入力欄10個を動的生成。各欄に店舗名サジェスト（既存の店舗登録画面と同じ/api/stores/suggestAPI）を付与し、保存時にCookie（favoriteStores）へ反映
public/index.html — お気に入り編集モーダルのマークアップと起動ボタン「お気に入り店舗を編集」を追加
public/css/style.css — 10行の入力欄レイアウト用スタイルを追加
あわせて、サジェスト機能が2画面（店舗選択・お気に入り編集）で必要になったため共通化しました。

public/js/store-suggest.js（新規） — サジェストAjax・ドロップダウン描画・選択処理を汎用部品化
public/js/favorites-util.js（新規） — お気に入りCookieの読み書き（最大10件・重複排除）を共通化
public/js/cookie-util.js（新規） — Cookie get/set/removeの共通化
public/js/store-select.js — 上記共通部品を使うようリファクタリング（重複コード削除）
public/js/main.js — 共通部品を使うよう更新、お気に入り編集ポップアップの起動・保存イベントを配線
ブラウザ実機で「サジェストから選択」「マスタに無い任意店舗名を入力」「空欄行は保存されない」「保存後Cookieに反映され店舗選択ポップアップのお気に入り一覧にも即反映される」「再度開くと保存済みの内容が復元される」ことまで確認済みです。

＝＝＝＝＝＝＝

・APIのURLを定数化して、jsファイルの先頭に持ってきてほしい。
・社内の本番APIと切り替えられるようにしたい。
・社内のサーバはログインが必要なので、社内アプリでログインしたときのcookie情報を手書きでどこかに書き、社内APIにテスト接続をしたい

API URLの定数化

public/js/api-config.js（新規）— クライアント側APIパスをAPI_ENDPOINTSとして定義し、index.htmlで最初に読み込むよう配置
store-suggest.js・store-select.jsの直書きURLをすべてAPI_ENDPOINTS.xxx参照に置き換え
server.js側にもROUTES定数を追加し、自サーバーが公開するパスを一箇所にまとめた
モック／社内本番APIの切り替え

ブラウザJSは仕様上Cookieヘッダを手動付与できないため、クライアントは常に自分のExpressサーバー（相対パス）だけを呼び、モード切り替えとCookie付与はサーバー側で行う構成にしました
config.js — API_MODE('mock'/'production')、本番ベースURL・パス・Cookieのデフォルト定義。config.local.jsがあれば自動でマージ
config.local.example.js — 手書き設定のテンプレート。コピーしてconfig.local.jsとして使う
.gitignoreにconfig.local.jsを追加（ログインCookieを誤ってコミットしないため）
社内APIへのテスト接続

config.local.example.jsをconfig.local.jsにコピー
社内アプリにログイン済みブラウザの開発者ツール(Network)からcookieヘッダの値をコピーしPRODUCTION_COOKIEに貼り付け
PRODUCTION_BASE_URLを実際の社内APIオリジンに書き換え、API_MODE: 'production'に
node server.jsで起動すると、/api/...へのアクセスがサーバー側で該当Cookie付きの本番APIリクエストに変換される（server.jsのproxyToProduction）
ダミーの社内APIサーバーを一時的に立てて、Cookieが正しく転送されること・Cookie不一致時は401がそのまま透過されることを確認済みです（検証用ファイルは削除済み）。現在はAPI_MODE=mock（既定）でhttp://localhost:3000が起動中です。

＝＝＝＝＝＝＝

この店舗選択とお気に入り登録ポップアップを社内アプリ（Spring Boot + Thymeleaf）に組み込む準備をしたい。
・ポップアップHTMLは th:replace でフラグメント埋め込みする方針でよいか？
・JSは public/js/cvs フォルダに分離してほしい。
・Ajax APIのURLにコンテキストパスへの考慮が抜けていた。画面側からコンテキストパスをつけて呼べるようにしたいが、モックAPI検証と両立できるか？
・その他、埋め込みをやりやすくする工夫は？

実施内容
1. th:replace 方針：問題なし。フラグメントは html/head/body を持たずモーダルdivだけ、scriptはホスト画面側で読み込む方針で integration/ に資材を用意。
2. JS分離：public/js/cvs-*.js の5本を public/js/cvs/ へ git mv。jquery-1.7.2.min.js と main.js（デモ専用）は据え置き。index.html の script src を js/cvs/... に更新。
3. コンテキストパス対応：新規 public/js/cvs/cvs-api-config.js に APIパス（config.paths）と CvsStoreWidget.util.apiUrl(path) を集約。
   従来 cvs-store-select.js / cvs-store-suggest.js に重複していた API_ENDPOINTS を廃止し apiUrl(PATHS.x) 参照に統一。
   画面が window.CvsStoreWidget.config.contextPath を先にセットしていれば尊重、未設定なら '' でモックは従来通り。
   社内アプリは cvs-api-config.js より前に th:inline スクリプトで @{/} を contextPath に渡す。末尾スラッシュは apiUrl が正規化。
4. その他：integration/cvs-store-select-popup.html（名前付き2フラグメント）、integration/README.md（コピー対象・読込順・イベントのペイロード形状・残課題＝CSRF/CSSスコープ/ID接頭辞/Cookie/認証/window.confirm）を追加。
   部分埋め込み向けに cvs-store-select.js / cvs-favorite-edit.js の初期化に `if (!$overlay.length) return;` ガードを追加。
   server.js のコメントを cvs-api-config.js 参照に更新（コード変更なし）。

確認：全 cvs JS の node --check OK。モック起動で /js/cvs/*.js が200、/api/cvs_chains・/api/stores/search が正常応答。
apiUrl を node で単体確認（contextPath 既定''／'/cvs-survey/'／'/cvs-survey'／'/'／事前セット'/preset/' すべて期待通り）。
ブラウザでのクリック操作確認はこの環境（chromium未導入）では未実施。

＝＝＝＝＝＝＝

組み込み中。th:replace とjsを反映して open() は呼べるがポップアップが出ない。
→ 切り分けの結果、オーバーレイは display:block だが position が static（.modal-overlay のCSS未適用/上書き）で、
  本文の一番下にインライン表示されてしまう。名前空間化してほしい。

実施内容（CSS名前空間化）
- 新規 public/css/cvs-store-widget.css：モーダル関連スタイルを全部ここへ集約。
  クラス名は cvs- 接頭辞（cvs-modal-overlay / cvs-modal / cvs-btn / cvs-form-row / cvs-suggest-* など）、
  全セレクタを .cvs-store-widget 配下にスコープ。box-sizing:border-box をウィジェット内で強制。z-index:100000。
- public/css/style.css：モーダル関連ルール（旧 .modal* 等）を削除し、上記ファイルへ誘導するコメントのみ残す。
  デモホスト用（.page/.panel/.btn/.result-table 等）はそのまま。
- public/index.html：<link> に cvs-store-widget.css 追加。2つのモーダルのルートdivに class="cvs-store-widget cvs-modal-overlay"、
  中のクラスをすべて cvs- 接頭辞へ。
- integration/cvs-store-select-popup.html：同じく名前空間化。置き場所の注意
  （position:fixed なので隠れコンテナや transform 祖先の中に置かない、body直下へ）をコメントに明記。
- JS：クラスを生成している箇所を cvs- 接頭辞へ（cvs-store-suggest.js の suggest-item/name/meta/empty、
  cvs-store-select.js の favorite-chip、cvs-favorite-edit.js の form-row/favorite-edit-row/autocomplete-wrapper/suggest-list）。
  ※JSはこれらクラスをセレクタで参照しておらず生成のみなので安全。
- integration/README.md：コピー対象に cvs-store-widget.css を追加、置き場所の注意、残課題からCSSスコープ項目を削除。

確認：全 cvs JS の node --check OK。モック起動で /css/cvs-store-widget.css が200、
index.html の <link> と cvs-store-widget/cvs-modal-overlay クラスを確認。

＝＝＝＝＝＝＝

CSSの読み込み不備を直してポップアップは表示できた。
cvs-store-widget.css には「ポップアップ成立に必要な要素」と「外観の要素」が両方入っている？
→ そのとおり。社内アプリはスマホ版/PC版で外観が違うので、構造と外観で分割してほしい。

実施内容（CSS分割）
- public/css/cvs-store-widget.css を廃止し、2ファイルに分割:
  - cvs-store-widget.layout.css … 構造のみ（box-sizing、overlay の position:fixed/flex/z-index、
    .cvs-modal の max-width/height:100%・overflow、header/footer の flex、select/input の width:100%、
    favorite-edit-row の flex、favorites-list の flex-wrap、autocomplete-wrapper の position:relative、
    suggest-list の absolute配置と<ul>リセット、suggest-item の flex）。
    暗幕と .cvs-modal / .cvs-suggest-list の background だけは「透け防止」の構造フォールバックとして残す。
    このファイル単体でも機能する（見た目は素っ気ない）。
  - cvs-store-widget.theme.css … 外観のみ（色・フォント・余白・角丸・影・ホバー、
    .cvs-modal の width:480px と max-width/height:calc、ボタン、hint/error/required、
    suggest の枠線/影、チップの見た目 など）。同一セレクタを後勝ちで上書き。
- 読み込み順は layout → theme。スマホ/PCで変えるときは theme を複製 or media 付き <link>、layout は共用。
- public/index.html：<link> を layout・theme の2本に。
- integration/README.md・フラグメントのコメント：コピー対象と読み込み順、SP/PC 出し分け例を追記。
- public/css/style.css：分離先コメントを2ファイル構成に更新。

確認：モック起動で layout.css / theme.css が200、index.html の <link> 2本を確認。

＝＝＝＝＝＝＝

既存の社内ポップアップは jQuery UI の dialog() で、引数でサイズ/タイトルを指定している。
現状の自作モーダルとの違いは？ → B案（中身を jQuery UI dialog に載せ替え）でやってほしい。
ブランチ 20260828_dialog。

実施内容（自作モーダル → jQuery UI dialog 版）
- 表示を $().dialog() に変更。フラグメント/index.html のモーダルは「中身だけ」に:
  <div id="storeSelectDialog" class="cvs-store-widget" title="店舗選択"> ... フォーム行のみ ... </div>
  タイトルバー・OK/キャンセル・暗幕・位置は jQuery UI が生成。
- cvs-store-select.js / cvs-favorite-edit.js:
  - $overlay → $dialog、cacheElements/initDialog/bindEvents に整理
  - initDialog: dialog({ autoOpen:false, modal:true, width:480, appendTo:'body',
      title, buttons:[{text:'OK',class:'cvs-dialog-confirm',click:handleConfirm},{text:'キャンセル',...}],
      open:focus, close:suggest hide })
  - open(arg): arg が文字列/数値なら file、オブジェクトなら { file,title,width,height,dialogOptions } を
    dialog('option', ...) で反映してから dialog('open')
  - close(): dialog('close')。自前の×/背景クリック/ESC 配線は撤去（jQuery UI 任せ）
  - 二重送信ガードは submitting フラグ＋ .cvs-dialog-confirm の disabled/ui-state-disabled トグルに変更
  - jQuery UI 未読込なら console.error で警告して初期化スキップ
- CSS: cvs-store-widget.layout/theme.css から .cvs-modal* / overlay / .cvs-btn* を削除し、
  「中身」だけのスタイルに縮小。box-sizing は .cvs-store-widget 配下へ再スコープ。
- モック用に jQuery UI 1.12.1 を同梱（jquery 1.7.2 と互換な最終版）:
  public/js/vendor/jquery-ui.min.js、public/css/vendor/jquery-ui.min.css + images/。
  package.json に devDependency と vendor:jquery-ui スクリプト追加。node_modules は .gitignore 済み。
- index.html: jQuery UI の <link>/<script> を追加、モーダルを中身だけの div に置換。
- integration/README.md・フラグメントのコメントを dialog 版に全面改訂
  （前提=jQuery UI、open() 引数でサイズ/タイトル、appendTo:'body' で置き場所は不問 等）。

確認：全 cvs JS の node --check OK。モック起動で jquery-ui.min.js/.css/icons が200、
index.html に storeSelectDialog/favoriteEditDialog と jQuery UI 参照を確認、旧クラス/IDの残存なし。
ブラウザでのクリック確認はこの環境（chromium未導入）では未実施。

＝＝＝＝＝＝＝

お気に入り登録を、社内アプリの「既存の別ダイアログ」に入力項目を追加する形にしたい。
どんな作業が必要？ → 保存先は Cookie 継続、単独ダイアログ版は撤去、埋め込み先は jQuery UI dialog。

実施内容（FavoriteEdit を単独ダイアログ → マウント式に）
- cvs-favorite-edit.js を書き換え。公開APIを差し替え:
  mount(target?, options?) … [data-cvs-favorite-edit-rows]（省略時）に入力行を生成＋サジェスト付与。
                             options.count で行数（既定 FavoritesUtil.MAX_COUNT=10）。冪等。
                             マウント先に .cvs-store-widget が無ければ付与（CSSスコープ用）。
  load()  … Cookie(favoriteStores) から各入力欄を復元。ホストダイアログの open で呼ぶ。
  save()  … FavoritesUtil.saveNames で Cookie 保存 → favorites-updated 発火 → 保存後配列を返す。OK で呼ぶ。
  getValues() … 現在値（trim 済み配列、保存なし）。
  reset() … 全行のサジェストを閉じる。close で呼ぶ。
  initDialog/open/close/toOpenOptions/自動初期化 は削除。入力欄IDは cvs-favorite-input-0..9 に。
  addFavoriteRow / resolvedNames / StoreSuggest / FavoritesUtil はそのまま再利用。
- フラグメント: favoriteEditPopup → favoriteEditFields（.cvs-store-widget ラッパ＋
  <div data-cvs-favorite-edit-rows> のみ）。ヘッダコメントも更新。
- index.html: favoriteEditDialog を廃止し、「既存ダイアログ」に見立てた #demoHostDialog
  （ダミー項目＋お気に入りセクション）に置換。
- main.js: #demoHostDialog を jQuery UI dialog 化し、mount()1回＋open→load / 保存→save / close→reset を配線。
- integration/README.md: お気に入りの節を「既存ダイアログへ項目追加」方式へ全面改訂
  （API表、jQuery UI dialog への配線例、cvs-store-widget クラス必須、overflow 注意）。
- cvs-store-select.js 他は無変更（FavoritesUtil.getNames() 依存のみ・Cookie 継続で無影響）。

確認：全 cvs JS＋main.js の node --check OK。stale な FavoriteEdit.open/favoriteEditPopup 参照なし。
モック起動で #demoHostDialog / data-cvs-favorite-edit-rows / 新API（mount/load/save/reset）を確認。
ブラウザ実クリック確認はこの環境（chromium 未導入）では未実施。

＝＝＝＝＝＝＝

組み込んだら、お気に入り側 dialog() の autoOpen/open/close が効かず、コメントアウトで動いた。この3つは何？
→ 初期化オプション。open/close はコールバック1個のみ（後勝ちで既存処理が消える）、
  autoOpen は初期化済みダイアログには無効。README のサンプルが「新規ダイアログ作成」前提だったのが原因。

実施内容（README/デモを「既存ダイアログへ混ぜる」書き方へ修正）
- integration/README.md 3-2: dialog({...}) に open/close/autoOpen を足す例をやめ、
  既存定義は触らず mount() 1回＋ $('#dlg').on('dialogopen'/'dialogclose', ...) でフックする例に変更。
  save() は既存の保存ボタン/submit ハンドラ内に1行追加。dialogbeforeclose で return false すれば閉じる中断も可、を補足。
- public/js/main.js: デモも同じ形へ。#demoHostDialog の dialog() 定義から open/close を外し、
  FavoriteEdit.load/reset は dialogopen/dialogclose イベントで配線。save は保存ボタン内のまま。
- cvs-favorite-edit.js ヘッダコメント: 呼びどころを dialogopen/dialogclose イベントに更新。

確認：main.js / cvs-favorite-edit.js の node --check OK。モックで dialogopen/dialogclose 配線を確認。

＝＝＝＝＝＝＝

組み込みで theme.css は使わず layout.css＋社内CSS 構成にした。サジェスト候補だけ layout のみ適用で
他のプルダウン（素の select）と見た目が違う。アプリCSSに追記するサンプルを組み込み手順に残してほしい。

実施内容（README のみ・コード変更なし）
- integration/README.md:
  - 「コピーするファイル」で theme.css を「任意」に。layout=構造(必須)/theme=外観の既定値(アプリCSSで代替可) と明記。
  - 2. の <link> に theme.css は任意のコメント。
  - 3-3「サジェスト候補の見た目をアプリに合わせる（theme.css を使わない場合）」を新設。
    .cvs-suggest-* に当てる最小CSSサンプル（枠線・行padding・hover=システムカラー・meta グレー）、
    詳細度メモ（ul. / > li. で埋め込み先の li リセットに勝つ）、幅は layout が left/right:0 で入力欄ぴったり、
    スマホ版は @media で調整、置き場所はアプリCSS（layout.css は書き換えない）。
- ※素の <select> の展開リストは OS 描画なので完全一致は不可、と明記。

＝＝＝＝＝＝＝

店舗選択ポップアップで一度確定した後、再オープン時に前回の店舗を選択済みにしたい。
呼び出し元が店舗IDを保持して渡し戻せる。検索APIに店舗ID条件を追加する方針。
決定: ID未ヒット時は「空で開く＋console.warn」、クエリパラメータ名は id。

実施内容
- cvs-store-select.js: open(arg) が opts.storeId（別名 opts.id_cvs_store）を受付。
  loadOptionsIfNeeded 後、storeId があれば preselectById() で
  $.getJSON(/api/stores/search, {id}) → 1件なら applyStore（＝候補選択と同じ確定状態）→ dialog open。
  未ヒット/失敗でも .always で必ず open（空のまま・console.warn）。
  既存の resetForm → loadOptionsIfNeeded の流れは維持。storeId 無しは従来どおり空。
- server.js: GET /api/stores/search に id 条件を追加。id 指定時は q を無視し、
  STORES ＋ registeredTargets（当該セッションの新規登録店）から id_cvs_store 完全一致で1件返す（配列）。
  production モードでも ?id= を素通しでプロキシ。
- main.js: 直近の store-selected の id_cvs_store を lastStoreId に保持し、
  次回 open({ file, storeId: lastStoreId }) で渡す。
- index.html: デモ可視化用に「再オープン時に渡す店舗ID」の表示行（#rememberedStoreId）を追加。
- integration/README.md 3-1: open の storeId 説明＋バックエンド要件（/api/stores/search が ?id= を受け、
  該当1件の配列を返す。id 指定時は q 無視）を追記。
- cvs-store-suggest.js は変更なし（?q= 専用のまま。ID取得は store-select が直接叩く）。

確認: node --check（cvs/*.js, server.js, main.js）OK。
モックで /api/stores/search?id=1 →1件、?id=99999 →[]、?id=1&q=渋谷 →id優先。
ブラウザ実クリックはこの環境（chromium未導入）では未実施。

＝＝＝＝＝＝＝

店舗登録API（PUT /api/stores）の戻りも records に包んだ形にしてほしい。
{ records: { id_cvs_store: 1, nm_cvs_store: ... } }（オブジェクト・配列ではない）。

実施内容
- server.js: PUT /api/stores のレスポンスを { success:true, id_cvs_store } → { records: record } に変更。
  record は採番済みIDを含む店舗全項目（＋file, registeredAt）。
- cvs-store-select.js: 新規登録成功時、response.id_cvs_store 直読みをやめ、
  registeredPayload = $.extend({ file: currentFile }, values, response.records) を組んで store-selected を発火。
  サーバー返却値（response.records）で上書きする形。
- integration/README.md: API節に「PUT /api/stores のレスポンスは { records: {店舗1件} }」
  「records に含める項目一覧」を明記。サジェストは配列（?q=/?id= 同形式）である点も追記。
- 既存店舗選択（API呼ばない分岐）は変更なし。

確認: node --check（cvs/*.js, server.js）OK。
モックで PUT /api/stores → { records: {...} } を確認。

＝＝＝＝＝＝＝

theme.css を外すと、お気に入り編集の行番号ラベル（1.〜10.）が可変幅になり
"10." だけ広がって入力欄が右にずれる。原因は theme.css の
.cvs-favorite-edit-row label { flex-basis: 20px }。

実施内容
- 列幅固定は装飾でなく構造なので layout.css へ移動。
  layout.css: .cvs-favorite-edit-row label を flex:0 0 auto → flex:0 0 20px。
  theme.css: flex-basis:20px を削除し、margin-bottom/color/font-weight のみ残す。
- これで theme.css 未使用でも番号列が揃う。theme 読込時も layout が唯一の幅指定（競合なし）。

＝＝＝＝＝＝＝

店舗登録時に user_id（登録者のユーザーID）パラメータを追加したい。
社内アプリでは cookie 'initNoEmp' から取得できる。

実施内容
- cvs-api-config.js: config.userIdCookie（既定 'initNoEmp'）を追加。
  値を直接渡したい場合は config.userId が優先。
- cvs-store-select.js: getUserId()（config.userId 優先→cookie(config.userIdCookie)）を追加。
  新規店舗登録の newPayload に user_id: getUserId() を追加（PUT /api/stores へ送る）。
  既存店舗選択（登録API非呼び出し）は変更なし。
- server.js: PUT /api/stores で user_id を受け取り record に格納（{records}レスポンスにも入る）。
- main.js: デモ用に cookie 'initNoEmp' が無ければ 'EMP0001' をセット。結果表に user_id を表示。
- index.html: 結果表に「登録者(user_id)」行を追加。
- integration/README.md: PUT /api/stores のbodyに user_id が入る旨と config.userIdCookie/userId を明記。

確認: node --check（cvs/*.js, server.js, main.js）OK。
モックで新規店舗登録 → PUT body に user_id、レスポンス records に user_id を確認。

＝＝＝＝＝＝＝


note:

netstat -ano | findstr :3000
taskkill /F /PID <上で表示されたPID>












